@model RestaurantList.Models.Restaurant

@{
    ViewData["Title"] = "Edit Restaurant";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 shadow-sm p-4 bg-white rounded">
            <h2 class="mb-4 text-warning">Edit Restaurant</h2>
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <input type="hidden" asp-for="Id" />

                <partial name="_RestaurantForm" />

                <div class="mt-4 border-top pt-3">
                    <button type="submit" class="btn btn-success px-4">Update Changes</button>
                    <a asp-action="Index" class="btn btn-outline-secondary ms-2">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}